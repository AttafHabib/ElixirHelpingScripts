# ElixirHelpingScripts

Clone the Repo
```
  git clone https://github.com/AttafHabib/ElixirHelpingScripts.git
```

In the cloned repo, run IEx shell
Run
    ```
    c ["Helpers.ex"]
    ```
    ```
    Helpers.getAssigns("TextFileName.txt")
    ```
